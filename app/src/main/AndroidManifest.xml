<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projet"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".InscriptionClient"
            android:exported="false">
        </activity>

        <activity
            android:name=".InscriptionBoutique"
            android:exported="false">
        </activity>

        <activity android:name=".Connexion" />

        <activity android:name=".FaqClient" />
        <activity android:name=".FaqBoutique" />

        <activity android:name=".AccueilClient" />

        <activity android:name=".ProfilBoutiqueInfos" />
        <activity android:name=".ProfilBoutiqueAcceuil" />

        <activity android:name=".Parametres" />
        <activity android:name=".Apropos" />
        <activity android:name=".MentionsLegales" />



    </application>

</manifest>