<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:fitsSystemWindows="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="#FFF5FB"
        android:gravity="center"
        android:paddingTop="24dp">

        <TextView
            android:text="@string/app_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center"
            android:textColor="#000000" />
    </LinearLayout>

    <!-- Form -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp"
            android:gravity="center_horizontal">

            <TextView
                android:text="@string/connexion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAlignment="center"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#000000"
                android:layout_marginBottom="24dp" />

            <!-- Identifiant -->
            <LinearLayout style="@style/FormFieldStyle">
                <TextView style="@style/FormLabelStyle"
                    android:text="@string/identifiant" />

                <EditText style="@style/FormEditStyle"
                    android:hint="@string/connexionEntrezID"
                    android:id="@+id/identifiant"/>
            </LinearLayout>


            <!-- Mot de passe -->
            <LinearLayout style="@style/FormFieldStyle">
                <TextView style="@style/FormLabelStyle"
                    android:text="@string/mdp" />

                <EditText style="@style/FormEditStyle"
                    android:hint="@string/mdpSecu"
                    android:inputType="textPassword"
                    android:id="@+id/mdp"/>
            </LinearLayout>

            <!-- Bouton -->
            <Button
                android:id="@+id/Connexion"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/seConnecter"
                android:textStyle="bold"
                android:backgroundTint="#D8A7D9"
                android:textColor="#000000"
                android:layout_marginTop="24dp" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
